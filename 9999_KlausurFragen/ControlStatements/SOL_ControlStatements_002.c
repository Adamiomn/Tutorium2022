for (size_t i = 5; i >= 0; --i)
{
	printf("%d\n", i);
}

// Wähle die richtige Aussage aus.
//		c) Die Schleife ist eine Endlosschleife.
// Erklärung:
//		c) i ist vom Typ size_t, was ein vorzeichenloser Integer ist (die genaue Größe ist abhängig vom Betriebssystem). Damit ist die Schleifenbedingung i >= 0 immer wahr, da i keine negative Zahl sein kann.