int f(int numbers[]);
int g(int * numbers);

// Gib an, ob die Aussagen jeweils wahr (w) oder falsch (f) sind.
//		a) ☐ Die Größe des Arrays ist in der Definition von f bekannt.
//		b) ☐ g akzeptiert sowohl Pointer auf int, als auch Arrays von ints.
//		c) ☐ Die Deklarationen von f und g sind (bis auf den Funktionsnamen) gleichbedeutend.