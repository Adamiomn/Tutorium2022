int * ptrNumber = 0;

// Gib an, ob die Aussagen jeweils wahr (w) oder falsch (f) sind.
//		a) ☐ Die Zuweisung ist gültig.
//		b) ☐ Der Ausdruck ptrNumber == NULL ergibt false.
//		c) ☐ Die Anweisung *ptrNumber = 10 ergibt einen Segmentation Fault (unerlaubter Speicherzugriff).