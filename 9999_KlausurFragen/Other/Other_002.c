const int i;

// Gib an, ob die Aussagen jeweils wahr (w) oder falsch (f) sind.
//		a) ☐ i ist ein konstanter Integer
//		b) ☐ Die Definition von i ist gleichbedeutend zur Definition int const i;
//		c) ☐ Die Zuweisung i = 5 ist gültig, da i nicht mit einem Wert initialisiert wurde.
//		d) ☐ Ein Pointer auf i muss vom Typ const int * const sein (z.B. const int * const ptr = &i)