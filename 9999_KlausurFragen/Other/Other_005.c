char const * int * ci;

// Wähle die richtige Antwort aus.
//		a) ci ist ein Pointer auf int, der auf einen Pointer eines konstanten char zeigt.
//		b) ci ist ein Pointer auf int, der auf einen konstanten Pointer eines char zeigt.
//		c) ci ist entweder ein Pointer auf int oder ein Pointer auf char, abhängig davon, mit welchem Typ initialisiert wird.
//		d) Der Typ von ci ist ungültig.
//		e) ci ist ein Pointer auf einen Integer, der einen Pointer auf char darstellt. Damit kann man negative Speicheradressen simulieren.